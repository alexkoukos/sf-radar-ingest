# Copy to .env.local for local dev. In production these are set in the
# Vercel dashboard, not committed here. The anon key is safe to ship in the
# bundle - RLS on the Supabase project restricts it to SELECT.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
